# EmailJS configuration

Service: `service_13mkb9h`

Contact template: `template_cvuls0n`

Auto-reply template: `template_4pj4xlm`

Verification template: configurable only.

Environment variables:

```env
VITE_EMAILJS_PUBLIC_KEY=YOUR_PUBLIC_KEY
VITE_EMAILJS_VERIFICATION_TEMPLATE_ID=
```

A missing verification template is an explicit unavailable state. Do not invent a template ID and do not claim verification delivery.
